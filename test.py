from core.util import screenshot, split, shifter
from core.Automata import Automata

# screenshot()
# left 248 right 172
# split("templates/t1.jpeg", (248, 0))

tester = Automata("", "")
tester.select_cards([6])