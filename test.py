from core.util import screenshot, split, get_crd
from core.Automata import Automata

# screenshot()
# left 248 right 172
split(screenshot(), (248, 0))

# tester = Automata("", "")
# tester.select_cards([6])